apiVersion: v1
kind: Service

metadata:
  name: gcp-cicd-app-service

spec:
  selector:
    app: gcp-cicd-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

  type: LoadBalancer
